// Intentionally empty: this component only exists to build and re-export the cspot
// library (see CMakeLists.txt). idf_component_register needs at least one source file
// for ${COMPONENT_LIB} to be a linkable static library rather than an INTERFACE target.
